# 新角色 AU · 交付清单模板

> 把这份清单填满 / 写好，一次性打包发过来即可部署。
> 分四块：**A 系列本体**、**B 画册条目（仅 Sam 本人演的角色）**、
> **C 专属色卡**、**D 画册·系列互链卡片（仅 Sam 角色）**。
> 不是 Sam Rockwell 本人演的角色，只填 A + C 即可，跳过 B 和 D。

---

## 先回答一个判断题（决定要不要填 B / D）

**这个角色是不是 Sam Rockwell 本人演的？**
- ✅ 是 → A + B + C + D 全填。
- ❌ 不是（是别的演员的角色，比如 Guy Pearce 演的 Leonard Shelby）→ 只填 **A + C**，B / D 跳过。
- 🤔 不确定演员是谁 → **先别猜，标一句「演员待确认」**，交付时告诉我，我来核。

---

# A · 系列本体（做系列首页 + 章节要用）

### A0. 封面图（⚠️ 单独走，别只在文字里提）

| 项 | 要求 | 你的值 |
|---|---|---|
| 封面图文件名 | 例 `Don-Verdean.jpg`。⚠️ 大小写 / 扩展名必须与真实文件**完全一致**（GitHub Pages 区分大小写） | |
| 图片上传到哪 | 约定：**由 Winter 通过 GitHub 网页上传到 `main` 分支的 `images/`**。写 AU 的对话**不用**把图片塞进交付包，只要把**文件名**告诉我即可。 | |

> 这张图会写进系列每一章的 `image:` 字段（全系列共用同一张）。
> **如果这张图还没上传**，务必在交付时点明「封面图待 Winter 上传，文件名暂定 ___」，
> 否则我部署时找不到图、卡片会是空的。

### A1. 基础信息（一张表，逐项给准确值）

| 项 | 要求 | 你的值 |
|---|---|---|
| 角色名 | 角色的名字（英文原名） | |
| 出处作品**官方全名** | ⚠️ 必须是完整官方名，不能用通称/简称，也不能用这个 AU 的时代设定顶替。拿不准先查证。 | |
| 演员 | 真人演员名（游戏角色无真人可省略；不确定就写「待确认」） | |
| 状态 | `ongoing` / `complete` / `oneshot` 三选一 | |

> 例：`Justin Hammer · Iron Man 2 · Sam Rockwell · ongoing`
> 反例：出处别写成「1660 复辟」这种时代设定，那属于 logline 和设定档案，不进这里。
> 特例：若**角色名恰好 = 片名**（如 Don Verdean 主演同名片《Don Verdean》），
> byline 会写成 `Don Verdean · Don Verdean · Sam Rockwell · ongoing`——**重复是正常的、正确的**，照写即可，别为了不重复去改。

### A2. 三个「必须完全一致」的英文串（内部管线，写错系列就空）

这三处必须是**同一个英文字符串**，且是纯英文（不能有中文/CJK）：

| 用在哪 | 你的值 |
|---|---|
| 系列英文名（`series_name` / 章节 `series:`） | |
| 系列目录 slug（= 上面那个名 slugify 的结果） | `series/______/` |

> 例：名 = `Good Enough`，slug = `series/good-enough/`。
> ⚠️ 中文只能出现在展示字段里，绝不能做这个英文名。

### A3. 展示标题（读者能看到的地方，统一格式 `Story · Character AU`）

**格式固定：`故事英文名 · 角色名 AU`，永远用角色名，绝不用剧名。**
一部电影可能开多个 AU，用剧名会撞车。

| 位置 | 格式 | 你的值 |
|---|---|---|
| 系列展示标题 | `Story · Character AU` | |
| 系列首页 hero 大标题 `<h1>` | **只写 Story**（不带 `· 角色 AU`），且必须英文 | |

> 例：展示标题 `The Far Side · Sam Bell AU`；hero `<h1>` 只写 `The Far Side`。

### A4. hero 情感引言（`c-hero__lede`，封面图上方那句）

- 一句**戳心的画面 / 悬念**，感性、诗意，常用第二人称「你 / 他」。
- 可以用中文。是「钩子」，不是简介，别剧透。

**你的引言：**
```
（写在这）
```

### A5. logline 基调卡片（封面图下方那张「基调 · Tone」卡）

- 1–2 句，**先点基调**（温馨治愈 / 暗涌 / 救赎向 / 科幻群像 / 轻喜…），再一句梗概。
- 语气**客观、克制、官方**，像内容简介——和上面 A4 那句感性引言**功能不同、别写重**。
- ⚠️ **值里严禁出现 ASCII 双引号 `"`**（会截断 YAML，整篇消失）。要引用改用中文「」或《》。

**你的 logline：**
```
（写在这）
```

### A6. 「关于这个系列」正文（系列首页第一段介绍）

- 2–4 段，概述世界观 + 核心冲突。
- 直接给**中文正文**即可（我来转成 HTML）。

**正文：**
```
（写在这，分段用空行）
```

### A7. 「设定档案」（可选，系列首页第二段）

- 人物简介 + 信物 + 关系动力学。
- 角色名后面带演员名，格式 `角色名（演员名）`；游戏角色可省略演员。

**内容：**
```
（可选，写在这）
```

### A8. 章节（每一章都要给下面这组）

每一章给我这些，我来生成 front matter + 正文：

| 字段 | 要求 | 你的值 |
|---|---|---|
| 章节序号 | 整数，从 1 开始 | |
| 章节中文标题 | 显示用 | |
| 眉头标签 | `Chapter N` / `Extra` / `Oneshot` 等 | |
| **summary 引言** | ⚠️ **≤35 个汉字（含标点）**、单句、**严禁 ASCII 双引号 `"`**。是钩子不是摘要。 | |
| **tags 完整标签** | ⚠️ 格式固定 `[角色名, 演员名, AU, 系列英文名, mood1, mood2]`——前四个是**身份标签**（全系列每章都一样），后面才是 1–2 个 mood。例 `[Don Verdean, Sam Rockwell, AU, Faith Unseen, 悸动, 暗涌]`。**别只给 mood。** | |
| **mood 标签** | ⚠️ 每章必加 **1–2 个**（就是 tags 末尾那两个），见下方清单 | |
| 正文 | 中文正文全文 | |

**可用 mood 标签（9 选 1–2，贴合该章主要情绪）：**
`缱绻`（缠绵温柔）· `思念`（想念距离）· `安放`（安心落定）·
`悸动`（初见心跳）· `暗涌`（危险张力）· `炽恋`（炽烈占有）·
`絮语`（日常轻快）· `怅惘`（遗憾错过）· `怀旧`（回忆留恋）
> Oneshot / 轻松番外一般选 `缱绻`+`絮语` 或 `悸动`+`缱绻`。长系列每章单独判断，别全系列同一对。

**正文里若出现「可以拿在手里读的实物」**（公文/声明/证书/判决/菜单 → 官方件；
字条/信/明信片/情书/便签 → 手写件），**告诉我一声**，我会做成卡片组件，别用裸加粗/引用。

---

# B · 画册条目（Many Faces of Sam）——**仅 Sam 本人演的角色**

> 画册是「写给 Sam Rockwell 银幕角色的情书集」，只收 **Sam 本人演过的角色**。
> 下面是画册里一张角色卡需要的**全部字段**，逐项给：

| 字段 | 要求 | 你的值 |
|---|---|---|
| `name` | 角色名（英文原名，可带昵称引号，如 `Buck "The Kid"`） | |
| `film` | 电影/作品英文**官方全名** | |
| `filmCN` | 作品中文名，带书名号 `《…》` | |
| `year` | ⚠️ 该作品**上映年份**（画册按年份升序排，必须准确） | |
| `tagline` | **一句话**角色介绍——画册封面卡上那句钩子。感性、抓人。 | |
| `desc` | 角色长介绍，**2–4 段**中文（世界观 + 角色 + 你们的关系，第二人称）。 | |
| `inscription.label` | 卡片背面刻印小标签，英文全大写（如 `OFF THE CLOCK`） | |
| `inscription.line` | 一句英文台词/心声 | |
| `inscription.gloss` | 上面那句的中文译注 | |

### B1. 六维性格打分（`profile`，雷达图用，各 0–10 的整数）

给这个角色在**六个维度**各打一个 0–10 的分：

| 维度 | 中文 | 含义 | 分（0–10） |
|---|---|---|---|
| guile | 狡黠 | 心机 / 算计 / 精明 | |
| forbear | 隐忍 | 隐忍 / 克制 / 承受 | |
| chaos | 疯狂 | 失控 / 混乱 / 危险 | |
| raw | 璞真 | 天真 / 本真 / 不设防 | |
| romance | 浪漫 | 深情 / 浪漫浓度 | |
| grace | 优雅 | 优雅 / 从容 / 风度 | |

> 例（Eric Knox）：狡黠10 · 隐忍6 · 疯狂8 · 璞真1 · 浪漫7 · 优雅9。凭角色气质给，不用纠结精确。

### B2.（可选）namePostfix / roleNote

角色有特殊说明才填，如 Sam Bell 的 `(pl.)`、客串角色的定位标注。没有就跳过。

---

# C · 专属色卡（每个角色/系列一套）——**A、B 都要用**

> 一套色卡 = **6 个颜色 + 4 个中英色名**。请按下表给全。
> ⚠️ 颜色请给**具体十六进制值**（如 `#4a87ee`）。**不确定就别编**——
> 可以只描述想要的气质（例：暖橘调、冷钢蓝、月光银白），交付时说明「配色待定」，
> 我会提示 winter 来定或按气质建议，绝不擅自替你编颜色。

| 字段 | 含义 | 你的值 |
|---|---|---|
| `accent` | 强调色（深色主题 + 色卡主色） | `#` |
| `bg` | 背景深色（整页底色） | `#` |
| `text` | 正文字色（落在 bg 上要清晰） | `#` |
| `muted` | 次级字色（日期 / 副标题） | `#` |
| `accent_cn` / `accent_en` | 强调色的中 / 英色名 | / |
| `bg_cn` / `bg_en` | 背景色的中 / 英色名 | / |

> 色名要有画面感，跟角色气质呼应。
> 例（Justin Hammer）：accent `#4a87ee`「电光钴蓝 / Spotlight Sapphire」，
> bg `#13151c`「落幕玄黑 / After-Curtain Onyx」。

**你不用管的两个派生值（我来算）：**
- `accent_ink`：`accent` 手动暗一档（约 80%），浅色主题下保证对比度。
- 画册色卡还会用到 `year`（= B 里那个上映年份），已在 B 收集。

---

# D · 画册 ↔ 系列 互链卡片（`sam_collection` + 画册 auLink）——**仅 Sam 角色**

> Sam 的角色，系列会出现在 **Sam tab 的 The Collection**，画册条目里也会挂一个
> 「跳去读这篇 AU」的链接。给我这几栏：

### D1. Sam tab 卡片（`sam_collection`）

| 字段 | 要求 | 你的值 |
|---|---|---|
| `collection_eyebrow` | 系列写 `AU Story · Series`；单篇写 `AU Story · Oneshot` | |
| `collection_title` | `Story · Character AU`（同 A3 展示标题） | |
| `collection_desc` | 一两句钩子简介 | |

> `collection_order`（排序）我来查当前最大值 +1，你不用管。

### D2. 画册条目里的 AU 链接（`auLink`，让画册跳到这篇 AU）

| 字段 | 要求 | 你的值 |
|---|---|---|
| `auLink.title` | `Story · Character AU` | |
| `auLink.label` | 一句钩子（可与 tagline 略不同，更偏「来读」的召唤） | |

> `auLink.href` 我按系列 slug 自动生成，你不用管。

---

## ✅ 交付前自查（30 秒）

- [ ] **封面图文件名给了**（或明确标注「待上传」）？大小写与真实文件一致？
- [ ] A2 三个英文串完全一致、纯英文？
- [ ] 所有展示标题都是 `Story · Character AU`、用**角色名**不用剧名？
- [ ] 出处作品是**官方全名**、不是简称/时代设定？
- [ ] 每章 summary **≤35 字**、无 ASCII 双引号 `"`？
- [ ] logline / summary 里没有 ASCII 双引号 `"`？
- [ ] 每章 **tags 是完整格式** `[角色, 演员, AU, 系列名, mood…]`、不只有 mood？
- [ ] 每章都有 **1–2 个 mood 标签**（tags 末尾那两个）？
- [ ] （Sam 角色）画册六维分、tagline、inscription 三件套齐了？
- [ ] 色卡 6 色 + 4 名给全了（或明确标注「配色待定」）？
- [ ] 判断题答了：这角色到底是不是 Sam 本人演的？

打包发来即可，我直接部署到线上。缺哪块我会点名回问，不用你猜。
