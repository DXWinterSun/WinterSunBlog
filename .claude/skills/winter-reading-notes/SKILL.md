---
name: winter-reading-notes
description: Winter 读英文原著（眼下是《A Single Shot》原著）时，把带荧光笔高亮的书页拍照发来——整理成「读书笔记」网页（Artifact），她在网页上直接批注、提问，Claude 在批注里回答，再把她的话和答案收进笔记；攒够了放上博客当她的读书笔记。当 Winter 发来书页 / 书本照片、提到高亮 / 划线 / 荧光笔 / 生词 / 词句笔记 / 读书笔记 / 原著笔记 / 整理成笔记 / 读到第几页，或在读书笔记网页上批注、提问，或说「笔记放博客」时，必须用本 skill。即使她只是甩来几张书页照片、一个字没说，只要照片里是带高亮的书页，也按本 skill 办。注意区分：AU 章节草稿预览用 tools/preview/render_draft.py（winter-au-writing）；本 skill 专管「书页照片 → 高亮词句笔记 → 批注 → 放博客」这一条线。
---

# 读书笔记：书页照片 → 高亮词句笔记 → 批注 → 放博客

2026-09-26 Winter 原话：**「我给你发书页的照片，里面会有高亮的词或短语，你能整理成笔记，
artifact 给我，我也可以批注，之后放在博客上可以做我的笔记，省得我自己写了……因为我在读
a single shot 的原著。」**

紧接着她纠正了一次，别再犯：**「不是在网页批注，是我们对话里像交付 au 章节一样的 artifact……
里面我可以直接给你批注问你问题。」**——「批注」＝ **Artifact 自带的批注功能**（选中一句 /
点卡片上的「批注」按钮 → 发给 Claude）。不要做数据库、不要做输入框、网页本身不存任何东西。

## 一句话流程

照片 → 读出每处高亮 → 写成笔记，追加进 `_data/reading/<书>.yml` → `render_notes.py` 生成网页 →
发布到**同一个链接** → 她在网页上批注 / 提问 → 你在批注里回她，把她的原话和答案收进那张卡 →
重新生成、重新发布 → 她说「放博客」时再排上博客。

## 文件都在哪

| 东西 | 位置 |
|---|---|
| 笔记数据（唯一真源） | `_data/reading/<slug>.yml`（A Single Shot 是 `a-single-shot.yml`） |
| 网页生成脚本 | `tools/reading/render_notes.py` |
| 固定文案 + 「先挑详略」样板卡 | `tools/reading/demo.yml` |
| 笔记网页的链接 | 数据文件里的 `book.artifact`（一本书一个链接，永远更新它） |

配色取 `_data/au_palettes.yml` 里 `book.palette` 那一套（A Single Shot 用 `Wintergreen`，
即 John Moon 的颜色），跟她看章节草稿的预览页是同一个页头样式。

## 一、读照片

1. **一处高亮＝一条笔记。** 同一句里划了几处，就拆成几条（原句重复出现没关系）；
   整句都划了，就以整句为词条。
2. **页码**从照片里读（页眉 / 页脚）。读不到就写 `page: ~`，并在 `unsure` 里说一声。
3. **原句一字不差**：大小写、标点、缩略（ain’t、’em）、不合语法的地方都照抄——人物说话本来就那样，
   别顺手「改对」。高亮部分用 `==…==` 包起来。一句跨到下一页、照片里没有后半句 →
   录到能看见的地方，末尾写 `……（接下页）`。
4. **看不清就说看不清**：猜出来的字写进 `unsure`（“第三个词被手指挡住，按上下文像是 ‘…’”），
   网页上会标「看不太清」请她确认。**绝不编一个像样的句子糊过去。**
5. **只录高亮所在的那一句**，不整页抄（仓库是公开的，整页抄书也不合适）。
6. 荧光笔颜色：第一次看到她的照片，把 `book.highlighter` 改成书上那支笔的颜色
   （淡一点：黄 `#e9cd6a` / 粉 `#e9a3b8` / 绿 `#a8d67a` / 蓝 `#8fc3e8` 一类）。

## 二、每条笔记怎么写

详略按 `book.detail`，由她在样板页上挑：

| 详略 | 卡片上显示 |
|---|---|
| `lite` 精简 | 词、音标、意思、原句 |
| `standard` 标准 | ＋ 词性、类型小标、整句大意、解读 |
| `full` 详细 | ＋ 英文释义、搭配 / 易混 |

字段写在 `batches[].notes[]` 里（批次本身写 `n`、`date`、`pages`、`photos`，可选 `remark`）：

```yaml
- id: "001"                 # 三位数，全书连号；永远不改、不复用（批注锚在卡片 id 上）
  page: 7
  term: hunkered down       # 书上高亮的原样
  lemma: hunker down        # 原形，跟 term 不同时才写
  kind: 短语                # 小标：单词 / 短语 / 习语 / 俚语 / 口语 / 方言 / 一词多义 / 名物 / 语法…
  ipa: /ˈhʌŋkər daʊn/       # 美式音标；整句或很长的短语可以不写
  pos: phr. v.
  cn: 蹲下身子；缩着不动     # 这句里的意思，不是词典全义
  en: to squat down low     # 仅 full：一句简单的英文释义
  sentence: "He ==hunkered down== behind the woodpile."
  gist: 他在柴火堆后面蹲下身子。   # 整句大意，一行
  note: ……                  # 解读：有话可说才写，见下
  extra: ["**搭配**……", "**易混**……"]   # 仅 full
  unsure: ……                # 看不清 / 拿不准时
  winter: ["……"]            # 她的批注，原话照录（见第四节）
  qa: [{q: ……, a: ……}]      # 她在批注里问的 + 你的回答
```

（上面是格式示意，不是书里的原句。）

写法规矩：

- **意思只给「这句里」的意思。** 常见义跟这里不一样时，在解读里点一句（“平时是……，这里是……”）。
- **解读有话可说才写**：方言和地方口语、俚语、乡下说法、不合语法的原因、打猎 / 枪械 / 农活之类的
  名物、文化背景、双关、语气。没什么可说的就空着，别凑字数。
- **拿不准的方言、俚语、名物先查证**，查不到就在解读里写“拿不准”，别编。
- ⚠️ **别剧透**：她还在往后读。解读只谈这一页和之前的内容，**绝不提后面的情节、结局、人物下场**，
  也别拿电影情节去对照还没读到的部分。
- 解读用中文写，**不中英夹杂**：英文只出现在被解释的词、原句和必要的对照词本身。
- 引号用弯引号 “ ”；页码、编号用阿拉伯数字。

## 三、生成、发布

```bash
python3 tools/reading/render_notes.py _data/reading/a-single-shot.yml -o <scratchpad>/a-single-shot-notes.html
```

- **一本书只有一个链接。** 新的一批往 `batches` **末尾**追加（`n` 递增），重新生成，发布到同一个链接：
  - 同一个对话里：用同一个 `file_path` 再发布即可；
  - 换了对话：先 `Artifact(action="read", url=book.artifact)`，再带 `url=book.artifact` 发布。
  - 旧的批次一个字都别挪——批注是锚在页面上的，前面的东西一动，旧批注就对不上位置。
- **第一次发布**要带 `capabilities: {"comments": {"composer_only": true}}`：卡片上的「批注」按钮
  靠它打开批注框。之后重新发布**不传** `capabilities`，保持原样。新书第一次发布后把链接写进 `book.artifact`。
- 数据里还没有任何一批时，页面显示「先挑详略」样板（`demo.yml`）；有了第一批，样板自动让位。
- 聊天里只给链接和几句人话（这一批几条、哪几条看不清要她确认），**不把笔记贴进聊天框**。
- 数据文件、脚本的改动跟着工作分支提交；`_data/reading/` 目前没有任何页面在用，推不推 main 都不影响线上。

## 四、她批注、提问之后

她把批注发给 Claude（「Send to Claude」或在批注里 @Claude）时，正在看这张页的对话会被唤醒。
换了对话，先用 `ArtifactComments(action="watch", url=…)` 接上；没被唤醒时也可以
`ArtifactComments(action="read", url=…)` 主动去读。每次：

1. 读全部批注，按锚点 / 引文对上是哪张卡（卡片 id 是 `n001` 这样的）。
2. **提问** → 在那条批注里直接回答：中文、简短、说人话，别剧透。有用的问答收进那张卡的 `qa`。
3. **批注 / 感想** → 原话一字不改收进那张卡的 `winter`（她写了几条就几条）。别替她润色，也别替她写。
4. **指出错误**（意思不对、原句抄错、页码不对）→ 改数据，在批注里回一句改了什么。
5. **定下偏好**（「就这版」「以后不要音标」「高亮换粉色」）→ 改 `book` 里的设置，并记进本文件第六节。
6. 重新生成、发布到同一个链接；回完、改完的批注串标成已解决（只能解决「发给了 Claude」的那些；
   没发给 Claude 的，告诉她哪几条还开着）。

## 五、放上博客

她说「放博客」时再做（2026-09-26 还没做）。动手前先给她看两三种形式挑，例如「一本书一页、越记越厚」
或「每读完一批发一篇」，她挑完再做。数据已经在 `_data/reading/` 里，Jekyll 直接能读
（`site.data.reading["a-single-shot"]`），样式照笔记网页来；上线照全站部署规矩（攒成一包推 main、
等构建跑完）。

## 六、她定过的偏好

- 2026-09-26：批注走 Artifact 自带的批注功能，网页里不另做存储。
- 2026-09-26：详略（A 精简 / B 标准 / C 详细）还没挑，先按 B；她在样板页上批注「就这版」或在聊天里说了之后改这里和 `book.detail`。
