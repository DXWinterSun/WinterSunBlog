# 账号级 skill 的仓库备份（快照，非在用版本）

这里是 Winter 账号里三个博客相关 skill 的**备份快照**（2026-07-18 由 Fable 存档）：

- `winter-blog-publishing` — 博客发布规范与部署流程
- `winter-many-faces-of-sam` — 画册角色的新增/修改规范
- `winter-au-illustration-prompt` — AU 配图 prompt 规范

**在用的正本在 Winter 的账号里**（跟着她进每个对话，包括没有本仓库的聊天对话），
平时**不要**读取或加载这个备份目录——它只在灾难恢复时用：如果账号里的 skill
意外丢失，把对应目录拷回去即可。

注意：
- 这是某个时间点的快照，账号正本后续可能有更新；恢复前想着提醒 Winter 这一点。
- `winter-au-writing`（AU 创作流程）情况相反：正本在本仓库 `.claude/skills/` 里，
  不在账号层——它需要读仓库里的系列圣经（`.claude/bibles/`），住仓库是对的。
- Winter 其余的个人 skill（教学、字幕、小红书等）含个人信息，**不放**进这个
  公开仓库备份。
